#include <stdio.h>

int main()
{
    int length, breadth;
    printf(" enter the valua of length\n ");
    scanf("%d", &length);
    printf("enter the value of breadth\n");
    scanf("%d", &breadth);
    printf("the area of the rectangle is %d", length * breadth);
    return 0;
}